# 이 파일을 .env 로 복사해서 필요한 값만 바꿔 쓰세요. (.env 는 커밋되지 않습니다)
# 기본값 그대로 쓸 거면 .env 없이도 동작합니다.

# 5433 포트가 이미 사용 중일 때만 변경
# DB_PORT=5434

# 8080 포트가 이미 사용 중일 때만 변경
# SERVER_PORT=8081

# DB_HOST=localhost
# DB_NAME=catchtable
# DB_USERNAME=catchtable
# DB_PASSWORD=catchtable
